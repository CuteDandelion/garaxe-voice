# Traceability Matrix

Status: Baseline
Last updated: 2026-07-14

| Requirement | Design/architecture | Verification |
|---|---|---|
| Evidence-backed insights | Signals -> themes -> insights links | E2E opens exact evidence from every published insight |
| Flexible data acquisition | Connector boundary + import mapping | CSV variants, mocked OAuth, staging provider proof |
| Reproducible analysis | Immutable run config + versioned stages | Rerun fixture equality and version audit tests |
| Human curation safety | Separate overrides/approval layer | Reanalysis does not overwrite curated output |
| Immutable reports | Versioned snapshot freezes run, curation, curated themes, machine provenance, and exact evidence | API immutability tests, valid PDF assertion, rendered-page inspection, and full-stack report publication |
| Multi-location value | Generic review entities + breakdowns | Location comparison fixtures and E2E |
| Garaxe visual fit | Editorial design system + local references | Visual review at desktop/mobile against captures |
| Accessible UI | Responsive/focus/reduced-motion contracts | Axe/manual keyboard/mobile checks |
| Tenant privacy | Application authorization + forced RLS + transaction-local database identity + encrypted credentials | HTTP cross-tenant concealment, least-privilege RLS behavior, and secret-log tests |
| Provider variability | Capability discovery + canonical review | Contract tests for pagination/full-text/refresh states |
| Cost-conscious analysis | Hybrid pipeline + incremental jobs | Per-stage usage metrics and 1k/10k review benchmarks |
| Quota-aware optional enrichment | Durable LLM queue, provider/model request and token buckets, atomic global/provider-model/organization concurrency admission, `Retry-After`, leases, fairness, retries, DLQ, and circuit breaker | Concurrent lease race proves one admission; saturation preserves request/token balances; two-organization and expired-lease tests; synthetic 429/5xx/reset tests; provider outage recovery |
| Optional hierarchical LLM budgets | Verified-price providers may enable global/organization/project/run integer-micro reservations and atomic reconciliation; capacity-priced jobs use zero reservation | Concurrent reservation tests, over-budget denial at every scope, ledger balance invariant, unverified-usage conservative charge, and zero-reservation dispatch without ledger writes |
| Idempotent model work | Stable tenant/run/input/prompt/schema/routing digest plus exactly-once result acceptance | Duplicate enqueue/lease-expiry/retry tests prove one accepted result and one reconciled charge |
| Safe deterministic fallback | Deterministic evidence artifacts remain authoritative; the default interpretation layer accepts only schema-, tenant-, taxonomy-, and exact-span-valid model candidates and records deterministic presentation as an explicit degraded fallback | Fifty-review acceptance fixture plus cross-tenant, unknown-review, invalid-taxonomy, bad-offset recovery, ambiguous/missing-span, duplicate, and size-limit validator tests; zero-key, over-budget, open-circuit, deadline, and dead-letter E2E remain queue integration gates |
| Exhaustive root-cause interpretation | Independent jobs cover every validated theme and all supporting feedback, interleave praise/pain priority, and return actionable aspect, praise/pain/mixed evaluation, evidence-backed multi-label signal types, cause before consequence, and exact supporting spans | Coverage/order contract test, zero-budget queue test, unsupported cause/consequence, unknown signal type, bad-offset tests, and 100-record food benchmark plus analyst comparison |
| LLM-first curation completion | Analysis remains non-curatable while interpretation jobs are active; accepted candidates drive curation vocabulary; deterministic presentation is an explicit terminal fallback | Run-settlement integration test, engine/version assertions, interpreted/fallback coverage assertions, full-feedback and exact-highlight component test |
| Observable LLM analysis | Durable queue/theme aggregation drives an accessible monitor immediately after **Run analysis**, with resume-after-navigation support and separate completion/interpretation coverage | API aggregation assertions, progress component test, live desktop and 390px Computer QA showing the same run advance |
| Local compact-model fallback | Provider-compatible local GGUF candidate uses the identical queue, prompt, validator, and candidate layer | Record artifact size/checksum/license, schema validity, unsupported-claim rate, p50/p95 CPU latency, peak RSS, and analyst preference before promotion |
| LLM privacy and auditability | Minimal provider payload, secret/PII-free operational events, immutable attempt and usage audit | Log/telemetry redaction tests, cross-tenant RLS tests, payload allowlist assertions, and retention/deletion verification |
| Cost-conscious delivery orchestration | Bounded delegation with primary-agent integration ownership | Review task partitioning, handoff evidence, duplicate work, and repeated test lanes |
| Cost-conscious model routing | Economy/standard/deep tiers with evidence-based escalation | Audit briefs for tier rationale, escalation trigger, and reusable decision handoff |
| Local Qwen default and paid-beta promotion | Versioned 50-review fixture, production cluster-schema fixture, bounded batches, explicit Qwen no-thinking local mode, compact field/evidence budgets, schema/span/taxonomy/performance scorer, and a separate target-environment promotion gate | Reasoning/default comparison, original-prompt truncation report, compact 12/12 pilot artifacts, and `model-evaluation.md` thresholds/results; paid-beta provider terms, latency, cost, and operational proof remain open |
| Reference-faithful Voice Map landing | React/Vite Voice Map Read default, no duplicate Overview navigation, responsive transformations | App/unit tests plus browser checks at 1536x1024 and 390x844 |
| Flexible upload/paste ingestion | Quoted CSV parser, XLSX/JSON/paste normalization, header aliases, manual mapping, source artifact and metadata preservation | Parser tests, real XLSX and File input tests, sample/paste UI flows, mobile overflow check |
| Import input integrity | Per-row rating-scale/date validation plus external-ID and normalized exact-text deduplication | CSV unit fixtures, server import integration, malformed-date/out-of-range-rating/duplicate-text Browser flow |
| Durable import provenance | Projects, import jobs, raw JSONB rows, normalized reviews, canonical hashes | In-memory database integration tests plus disk-backed full-stack Browser import |
| Inspectable review inventory | Project-scoped inventory, filter summaries, cursor pagination, lazy provenance detail | API filter/pagination/provenance tests and full-stack import/filter/drawer Browser flow |
| Immutable analysis dataset | Frozen run configuration plus one membership row per project review | API snapshot/membership tests and full-stack completed-run report |
| Deterministic preprocessing | Original/normalized separation, versioned exclusion precedence, duplicate groups | Nine focused preprocessing fixtures plus quality-report integration tests |
| Exact evidence spans | Versioned review signals with original offsets and theme-evidence joins | Signal fixtures plus API substring equality assertions and evidence drawer Browser flow |
| Dataset-derived aspect discovery | Deterministic recurrence extraction supplements governed rules without using metadata or a fixed industry taxonomy | Synthetic unseen-aspect unit fixture and 100-record food-industry rerun |
| CPU semantic theme discovery | Exact clause segmentation -> pinned multilingual sentiment q8 -> polarity-specific multilingual-e5-small q8 mutual-KNN graph -> coherence gates and outliers -> one independent-review representation per accepted cluster | Exact-offset/polarity/mutual-neighbour/outlier/coherence unit tests, 100-record unseen fixture, rerun equality, diagnostic visibility, and 4 vCPU/8 GB latency/RSS benchmark |
| Cost-bounded grouping review | Deterministic ambiguity flags plus `keep`/`split` assessment inside the existing LLM interpretation call | Validator rejects unsolicited/unsupported splits; strict Curation projection quarantines unresolved splits; no new job kind or provider request is created |
| Publication-quality interpretation | Versioned LLM `publish`/`discard` and `keep`/`split` dispositions suppress metadata, context, template-joined topics, missing interpretations, and unresolved mixed clusters while retaining immutable evidence | Adversarial shared-boilerplate validator fixture, unreasoned-discard rejection, Voice Map suppression, and strict LLM Curation queue integration |
| Full-context evidence | Voice Map response includes full original text, validated offsets, source dimensions, and review traversal | API substring assertion, highlighted-mark component test, Computer/browser drawer traversal |
| Accessible evidence buckets | Top-eight descriptive theme-name bubbles sized by independent supporting reviews, radius-scaled type, perceptible boundary/collision physics, interaction pause, reduced-motion static layout, keyboard/tap, semantic table fallback | Component label/count/radius/font/motion/keyboard tests, live transform and non-overlap check, reduced-motion browser check, 390px readability/overflow QA |
| Validated deterministic themes | Independent review support, contradictions, breakdowns, thresholds, confidence | Theme-engine fixtures and ranked Investigate UI |
| Evidence-grounded Voice Map | Insight and recommendation theme IDs plus immutable synthesis artifact | API artifact integration, Read mode, and exact-theme drawer traversal |
| Dedicated signal investigation | Evidence-backed multi-label project themes with ready-curation projection and exact evidence | Adapter routing tests prove one theme can populate Pain, Objections, and Emotional Triggers; application tests cover all four project-backed routes plus keyboard-contained evidence, exact source-review traversal, and dialog focus restoration |
| Project-correct application shell | Review summary and latest analysis drive sidebar counts/confidence, review period, and active workspace title | App assertions plus live 50-review desktop/mobile Browser verification |
| Authorized project and session controls | Membership-scoped project list, synchronized selectors, authenticated identity, and revoking logout | App interaction tests plus live second-project switch, logout, and local-session resume Browser proof |
| Auditable date-window analysis | Global From/To control creates a new versioned run with frozen configuration and refreshes Voice Map from completion | App request-contract test plus live bounded-date run and changed evidence-period Browser proof |
| Evidence-backed Copy Lab | Deterministic format/tone drafts derived from a validated theme and exact excerpts | App route test proves project evidence basis; zero-provider state remains functional |
| Human-approved publication | Run-scoped append-only curation actions, readiness gate, derived published projection | Twelve API integrations, controlled workspace tests, and full-stack edit/pin/approve/reject/publish Browser flow |
| Report decision utility | Immutable v2 snapshot stores source counts, evidence-cited LLM guidance, deterministic charts, prioritized actions, full original comments, and transparent synthesis provenance | Narrative validator tests, report API snapshot assertion, valid PDF/text extraction, five-page visual inspection, and desktop/mobile report verification |
| Authenticated organization boundary | Hashed opaque sessions, membership roles, project-organization ownership, authorization on every resource | Auth unit tests plus unauthenticated and cross-tenant HTTP integration |
| Managed PostgreSQL portability | Shared database interface, node-postgres pool adapter, checksum migration runner, and forced tenant RLS | Parameter/transaction/release adapter tests plus idempotent policy and least-privilege isolation tests; live managed rehearsal remains gated |
| Google connector readiness | Injected token/fetch boundary, account/location/review pagination, canonical DTO, safe capability/error contract | Thirteen mocked provider contract tests; approved-project staging proof remains gated |
| Multi-format source fidelity | Original artifact bytes/hash plus normalized CSV mapping boundary | Real rendered XLSX, JSON/paste fixtures, server artifact equality, and import UI tests |
| Google authorized ingestion | PKCE OAuth, encrypted tokens, persistent entities, explicit selection, frozen sync job, raw payload retention | OAuth/security tests, five sync persistence tests, and protected HTTP discovery-to-inventory integration |
| Local auth continuity | One-time owner bootstrap plus non-production loopback recovery | AuthGate interaction tests and fresh-server Browser setup/recovery proof |
| Staging auth continuity | Explicit staging tier plus generated access key for one configured existing owner; normal opaque session after constant-time verification | API wrong-key/no-secret-return integration and AuthGate staging-route interaction test; live rotation and browser proof remain deployment evidence |
| Bluerose staging deployment | Compiled non-root images, prewarmed offline models, retained PostgreSQL, migration Job, ClusterIP services, NetworkPolicy, Cloudflare Tunnel/DNS, immutable SHA tags | Local container build/smoke, remote manifest validation, live migration/RLS/persistence/restart, public TLS/content checks, Portfolio preservation, and rollback rehearsal |
| Test data isolation | Vitest-wide in-memory database selection before lazy initialization | Server suite plus unchanged local database mtime assertion |
